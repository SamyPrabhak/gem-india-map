# Make the Map Frame Flush

- Remove the map container's own rounded border and shadow so it does not create a second inset edge.
- Keep the ornamental gold frame as the single visible border around the map.
- Clip the map directly to the frame's inner edge with no ivory gap.
- Verify the result on the homepage at desktop and mobile sizes, including map interaction.
